import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-jetbrains",
  display: "swap",
});

export const metadata: Metadata = {
  title: "CodePulse — Multi-Agent Codebase Intelligence",
  description:
    "Submit a local repository and get a production-grade engineering report in seconds. Powered by 6 parallel LLM agents, Tree-sitter AST analysis, and grounded static analysis.",
  keywords: ["code analysis", "codebase intelligence", "LLM", "static analysis", "engineering report"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
